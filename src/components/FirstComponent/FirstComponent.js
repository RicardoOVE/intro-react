import React from "react";

/** Componente de Clase */
class FirstComponent extends React.Component{

    /* Función render es la encargada de renderizar el contenido que colocamos en el return */

    render (){
        return(
            <div>
                <h2>Este es el primer componente de clase</h2>
            </div>
        );
    }

}

export default FirstComponent